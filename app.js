const app=document.querySelector('#app'),breadcrumb=document.querySelector('#breadcrumb');let library=[],state={subject:null,course:null};const esc=v=>String(v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c])),courseLabel=i=>i.course||'未分类课程';function crumbs(){const p=[['全部学科',()=>state={subject:null,course:null}]];if(state.subject)p.push([state.subject,()=>state={subject:null,course:null}]);if(state.course)p.push([state.course,null]);breadcrumb.innerHTML=p.map(([n,a],i)=>`${i?'<span>/</span>':''}${a?`<button data-crumb="${i}">${esc(n)}</button>`:`<span class="current">${esc(n)}</span>`}`).join('');breadcrumb.querySelectorAll('[data-crumb]').forEach(b=>b.onclick=()=>{p[b.dataset.crumb][1]();render()})}function card(n,c,i){return `<button class="card" data-name="${esc(n)}"><span class="number">${String(i+1).padStart(2,'0')} / ${c} 条资源</span><h2>${esc(n)}</h2><p>点击进入下一层内容</p><span class="arrow">↘</span></button>`}function render(){crumbs();if(!state.subject){const g=Object.groupBy(library,i=>i.subject);app.innerHTML=`<div class="grid">${Object.entries(g).map(([n,x],i)=>card(n,x.length,i)).join('')}</div>`;app.querySelectorAll('[data-name]').forEach(b=>b.onclick=()=>{state.subject=b.dataset.name;render()})}else if(!state.course){const g=Object.groupBy(library.filter(i=>i.subject===state.subject),courseLabel);app.innerHTML=`<div class="grid">${Object.entries(g).map(([n,x],i)=>card(n,x.length,i)).join('')}</div>`;app.querySelectorAll('[data-name]').forEach(b=>b.onclick=()=>{state.course=b.dataset.name;render()})}else{const p=library.filter(i=>i.subject===state.subject&&courseLabel(i)===state.course);app.innerHTML=p.length?`<div class="prompt-grid">${p.map(i=>`<article class="prompt"><div class="preview">${i.status==='ready'&&i.video?`<video controls preload="metadata" src="${esc(i.video)}"></video>`:'<span class="generating">VIDEO GENERATING</span>'}</div><div class="prompt-body"><span class="status">${i.status==='ready'?'VIDEO READY':'生成中'}</span><h2>${esc(i.title)}</h2><div class="tags">${(i.tags||[]).map(t=>`<span class="tag">${esc(t)}</span>`).join('')}</div><div class="prompt-text">${esc(i.prompt)}</div><a class="cta" href="https://leadde.ai" target="_blank" rel="noreferrer">需要同类视频？前往 leadde.ai 制作 →</a></div></article>`).join('')}</div>`:'<p class="empty">该课程暂未添加 Prompt。</p>'}}fetch('./data/prompts.json').then(r=>r.json()).then(d=>{library=d;render()}).catch(()=>app.innerHTML='<p class="empty">无法读取资源数据。</p>');
